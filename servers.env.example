# 服务器列表配置示例
# 格式：服务器地址:部署路径
# 如果不指定路径，默认使用 /srv

# 示例 1：只指定服务器地址（使用默认路径 /srv）
# gpu

# 示例 2：指定服务器地址和路径
# gpu:/srv
# worker-01:/home/user/trap-factory

# 示例 3：多个服务器
# gpu:/srv
# worker-01:/srv
# worker-02:/srv
# worker-03:/srv

# 注释行（以 # 开头）和空行会被忽略
